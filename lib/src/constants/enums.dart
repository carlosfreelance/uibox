enum GridPrefixes { xxl, xl, lg, md, sm, xs }

enum UiBoxVariant {
  primary,
  secondary,
  success,
  danger,
  warning,
  info,
  light,
  dark,
  custom
}

enum UiBoxButtonVariant { filled, outlined }

enum UiBoxFadeInVariant { fadeInLeft, fadeInRight, fadeInTop, fadeInBottom }

enum UiBoxFadeOutVariant {
  fadeOutLeft,
  fadeOutRight,
  fadeOutTop,
  fadeOutBottom
}

enum UiBoxSlideVariant { slideInTop, slideInBottom, slideInLeft, slideInRight }

enum UiBoxZoomVariant { zoomIn, zoomOut }

enum UiBoxBounceVariant {
  bounceInTop,
  bounceInBottom,
  bounceInLeft,
  bounceInRight
}

enum UiBoxFlipVariant { flipX, flipY }

enum UiBoxBreadCumbVariant { wrap, scroll }
